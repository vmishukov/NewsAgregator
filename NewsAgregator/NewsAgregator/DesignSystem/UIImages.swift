import UIKit

extension UIImage {
    static let inlyNewsTab = UIImage(systemName: "newspaper") ?? UIImage()
    static let inlySelectedNewsTab = UIImage(systemName: "heart.text.square") ?? UIImage()
    static let inlyChevronRight = UIImage(systemName: "chevron.right") ?? UIImage()
    static let inlyArticleMock = UIImage(named: "NewsArticleMock") ?? UIImage()
    static let inlyArticlePlaceholder = UIImage(systemName: "newspaper.fill") ?? UIImage()
    static let inlySuitHeartFill = UIImage(systemName: "suit.heart.fill") ?? UIImage()
}

